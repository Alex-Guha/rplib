{
  "name": "rplib-workspace",
  "private": true,
  "description": "Workspace root for the rplib core library, rplib-editor authoring UI, and rplib-viewer read-only UI.",
  "workspaces": [
    "core",
    "editor",
    "viewer"
  ],
  "scripts": {
    "test": "npm test --workspace rplib"
  },
  "license": "ISC"
}
